import React from "react";

const Profile: React.FC = () => {
    return (
        <div style={{
            display: 'flex',
            justifyContent: 'center',
            alignItems: 'center',
            height: '100vh',
        }}>
            <h1>Profile Page</h1>
        </div>
    );
};

export default Profile;